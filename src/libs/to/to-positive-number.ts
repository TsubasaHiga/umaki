/**
 * Converts a given number to a positive number.
 *
 * @param n - The number to be converted. If the number is negative or zero, it will be converted to its absolute value.
 * @returns The positive number.
 */
export const toPositiveNumber = (n: number): number => Math.abs(n)
